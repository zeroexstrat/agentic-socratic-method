# premerge/ — one-time advisory input to the merge (NOT part of the baton)

MERGE MODE deliverable: a new union notebook (`product/product_i0.ipynb` onward).
Before OPUS48 authors the first merge draft, SOL56 (Codex) gives 99c an independent
read and drops an advisory critique here.

- `99c_advisory_SOL56.md` — written by Codex. **Advisory only: no LEDGER rows** (its
  cell indices are 99c-relative and won't map to `product_i0`). OPUS48 consumes it as
  input to the merge; those concerns become real, product-indexed LEDGER findings only
  when Codex audits `product_i0` at iter 0.

Instruction Codex runs: `prompts/premerge_99c_SOL56.md`.
This directory is outside CONTRACT.md's phase structure — a human-authorized pre-step,
logged in LEDGER.md's STATE LOG.
